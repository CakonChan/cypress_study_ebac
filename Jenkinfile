pipeline {
  agent any
  stages {
    stage('Instalar deps') {
      steps {
        sh 'npm install'
      }
    }
    stage('Executar testes WEB') {
      steps {
        sh 'NO_COLOR=1 npm test'
      }
    }
  }
}