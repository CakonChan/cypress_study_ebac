pipeline {
  agent any
  environment { NO_COLOR = '1' }
  stages {
    stage('Instalar deps') {
      steps {
        sh 'npm install'
      }
    }
    stage('Executar testes WEB') {
      steps {
        sh 'npm test'
      }
    }
  }
}