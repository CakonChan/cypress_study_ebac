pipeline {
  agent any
  stages {
    stage('Instalar deps') {
      steps {
        bat 'npm install'
      }
    }
    stage('Executar testes WEB') {
      steps {
        bat 'NO_COLOR=1 npm start'
      }
    }
  }
}